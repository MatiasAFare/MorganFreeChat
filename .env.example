# === SERVIDOR ===
PORT=3000
WS_PORT=8080
NODE_ENV=development

# === SEGURIDAD (REQUERIDO - GENERAR NUEVOS VALORES) ===
# Generar con: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your_random_jwt_secret_here_change_this
ENCRYPTION_KEY=your_random_encryption_key_here_change_this
JWT_EXPIRES_IN=24h
BCRYPT_ROUNDS=12

# === BASE DE DATOS ===
DATABASE_PATH=src/data/users.db

# === LOGGING ===
LOG_LEVEL=info
LOG_DIR=src/logs
LOG_FILE_CHAT=chat-messages.log
LOG_FILE_SYSTEM=system.log
LOG_MAX_SIZE=10m
LOG_MAX_FILES=14

# === CLIENTE ===
CLIENT_RECONNECT_INTERVAL=5000
MAX_MESSAGE_LENGTH=1000

# === WEBSOCKET (OPCIONALES) ===
WS_HEARTBEAT_INTERVAL=30000
WS_TIMEOUT=5000
WS_MESSAGE_QUEUE_LIMIT=100
